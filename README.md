# OpenFeature Healthcare API

This is a healthcare backend API built using TypeScript and Express.js.

It uses OpenFeature to control premium chat access for users.


## How to Run

1. Install dependencies:
   npm install

2. Run the server:
   npx ts-node main.ts

3. Open in browser:
   http://localhost:4000
